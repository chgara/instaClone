import { Dimensions } from 'react-native';

export const width = Dimensions.get('window').width;
export const height = Dimensions.get('screen').height;
export const imageSize = width / 5.85;
export const borderSize = width / 5.5;
